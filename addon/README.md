# PulseSync Addon Template

Пример `script`-аддона для PulseSync.

Стили подключаются через `script.css`, настройки читаются из `handleEvents.json`.

Файлы:

- `script.js`
- `script.css`
- `handleEvents.json`
- `metadata.json`
